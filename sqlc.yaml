version: 1
packages:
  - name: "db" 
    engine: "postgresql"
    queries: "./db/query/"
    schema: "./db/schema/"
    path: "./db/sqlc"
    emit_json_tags: true
    emit_prepared_queries: false
    emit_interface: false
    emit_exact_table_names: false

